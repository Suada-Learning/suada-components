import { ReactElement } from 'react';
import { ButtonState } from './Button.interface';
export declare const Button: ({ type, size, label, iconLeft, iconRight, disabled, customStyles, onClick, htmlType, className, themes, }: ButtonState) => ReactElement;
