import { ReactElement } from 'react';
import { ISplashScreenProps } from './splashScreen.interface';
export declare const SplashScreen: ({ title, subTitle, createButton, marginTop, minHeight, height, contentMaxWidth, textMarginTop, isSearchOrFilter, }: ISplashScreenProps) => ReactElement;
