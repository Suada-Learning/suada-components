import { ReactElement } from 'react';
import { ITileProps } from './tile.interface';
export declare const Tile: ({ customCardStyles, $backgroundColor, iconColor, icon, gap, value, subIcon, text, }: ITileProps) => ReactElement;
