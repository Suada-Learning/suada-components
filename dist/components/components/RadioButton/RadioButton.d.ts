import { ReactElement } from 'react';
import { IRadioButtonProps } from './radioButton.interface';
export declare const RadioButton: ({ checked, value, onChange }: IRadioButtonProps) => ReactElement;
